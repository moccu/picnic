sudo: false
language: node_js
node_js:
- 4
before_install:
- npm install -g grunt-cli
- npm install -g jspm
- jspm config registries.github.auth $JSPM_GITHUB_AUTH_TOKEN
install:
- npm install
- jspm install
- jspm install --dev
